<!--header -->
<nav class = "header">
  <div class = "nav-left">
    <h1>Potepan Reservation</h1>
  </div>
  <div class = "nav-right">
    <!-- サインインされている場合 -->
    <% if user_signed_in? %>
      <ul>

        <!-- ドロップダウンを随時追加 -->
          <li class="logout"><a class="dropdown-item" >
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete%></a>
          </li>
      </ul>
    <!-- サインインされていない場合 -->
    <% else %>
      <div class = "user-register">
          <%= link_to "ログイン", new_user_session_path, class: 'post' %>
          <%= link_to "新規登録", new_user_registration_path, class: 'post' %>
      </div>
    <% end %>
  </div>
</nav>

